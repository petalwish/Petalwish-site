---
import BaseHead from '../components/BaseHead.astro';
import Hero from '../components/hero/Hero';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const sections = [
	{ id: 'hero', label: 'Hero' },
	{ id: 'lab-clusters', label: '核心展厅' },
	{ id: 'blog', label: 'Blog' },
	{ id: 'outro', label: '履历&联系' },
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body class="home-page">
		<div class="home-bg" aria-hidden="true"></div>

		<header class="home-topbar">
			<a href="/" class="topbar-logo-slot" aria-label="首页">
				<span class="topbar-logo-dot" aria-hidden="true"></span>
			</a>
			<nav class="topbar-nav" aria-label="Homepage sections">
				{
					sections.map((section) => (
						<a href={`#${section.id}`} class="topbar-link">
							{section.label}
						</a>
					))
				}
			</nav>
		</header>

		<main class="home-main">
			<section id="hero" class="home-section home-section-hero" aria-label="首屏">
				<Hero client:only="react" />
			</section>
			{sections.filter((s) => s.id !== 'hero').map((section) => <section id={section.id} class="home-section" />)}
		</main>
	</body>
</html>
